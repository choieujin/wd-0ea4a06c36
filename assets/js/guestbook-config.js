/**
 * 축하 메시지(방명록) — 구글 폼 + 구글 시트 설정
 *
 * 여기 세 군데만 채우면 GitHub Pages 에서도 하객이 메시지를 남길 수 있다.
 * (서버를 띄울 필요도, Apps Script 를 배포할 필요도 없다)
 *
 *   쓰기 : 구글 폼으로 전송  →  연결된 구글 시트에 자동으로 쌓임
 *   읽기 : 그 시트를 그대로 읽어와 화면에 표시
 *
 * ─────────────────────────────────────────────────────────────────────
 * 준비 (한 번만)
 *
 *  ① 구글 폼을 만든다 — 질문 2개, 순서대로
 *       1) 이름          : 단답형
 *       2) 축하 메시지   : 장문형
 *     (로그인 요구·이메일 수집은 모두 꺼둘 것. 하객이 로그인 없이 써야 한다)
 *
 *  ② 폼의 [응답] 탭 → 스프레드시트 아이콘 → "기존 스프레드시트 선택"
 *     → 지금 쓰는 시트를 고른다. 이제 응답이 그 시트에 쌓인다.
 *
 *  ③ 시트를 [공유] → "링크가 있는 모든 사용자" → 뷰어 로 바꾼다.
 *     (읽기용. 뷰어라서 남이 고칠 수는 없다)
 *
 *  ④ 아래 formAction 과 fields 를 채운다.
 *
 *     • formAction : 폼 [보내기] → 링크 주소에서 끝의  /viewform  을  /formResponse  로 바꾼 것
 *         예) https://docs.google.com/forms/d/e/1FAIpQL.../formResponse
 *
 *     • fields     : 폼 우측 상단 ⋮ → "미리 채워진 링크 가져오기" → 아무 값이나 입력 후
 *                    [링크 가져오기] → 복사된 주소 안의  entry.숫자  두 개를 옮겨 적는다.
 *         예) ...viewform?entry.1234567=홍길동&entry.7654321=축하해요
 *             → name: "entry.1234567",  message: "entry.7654321"
 * ─────────────────────────────────────────────────────────────────────
 *
 * 안 채워도 사이트는 정상 동작한다. 비어 있으면 방명록이 "읽기 전용"으로 표시된다.
 */
window.GUESTBOOK = {

  /* ①②④ 구글 폼 — 메시지를 받는 곳 (비우면 읽기 전용) */
  formAction: "",
  fields: {
    name: "",     // 예: "entry.1234567"
    message: ""   // 예: "entry.7654321"
  },

  /* ③ 구글 시트 — 메시지를 읽어오는 곳 */
  sheetId: "1mpptL_y84YMG4zQS2SMu7ne0HsTtO84Y11NVHmfvNqo",
  sheetName: "",  // 탭 이름. 비우면 첫 번째 탭 (보통 "설문지 응답 시트1")

  /* 예비용 — 위 방법으로 목록이 안 읽히면 여기를 쓴다.
     시트 [파일] → [공유] → [웹에 게시] → 형식 "쉼표로 구분된 값(.csv)" → 게시
     → 나온 주소를 그대로 붙여넣기 (그러면 그 주소를 우선 사용한다) */
  csvUrl: ""
};
